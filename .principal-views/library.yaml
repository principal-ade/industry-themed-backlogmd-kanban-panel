version: "1.0.0"
name: "@industry-theme/backlogmd-kanban-panel"
description: "Kanban board panel for visualizing Backlog.md tasks in the industry-themed panel framework"

resources:
  # Production/library context - matches the instrumentation scope
  backlogmd-kanban-panel:
    service.name: "@industry-theme/backlogmd-kanban-panel"
    service.version: "1.0.41"

  # Storybook/testing context
  backlogmd-kanban-panel-storybook:
    service.name: "industry-themed-backlogmd-kanban-panel-storybook"
    service.version: "1.0.41"
    deployment.environment: "development"
    project: "backlogmd-kanban-panel"
    owned-scopes:
      - "@industry-theme/backlogmd-kanban-panel"

nodeComponents: {}

edgeComponents:
  interaction:
    description: "User interaction with UI component"
    color: "#3b82f6"
  trigger:
    description: "Event triggers another action or event"
    color: "#8b5cf6"
  emit:
    description: "Emits an event to the event bus"
    color: "#ec4899"
  display:
    description: "Updates or displays UI"
    color: "#10b981"
  flow:
    description: "Data or control flow between processes"
    color: "#f59e0b"
  call:
    description: "Function or API call"
    color: "#06b6d4"
  update:
    description: "Updates state or data"
    color: "#84cc16"
